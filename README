Before running setupHost.sh, cop the host_prompt_colors.dist file to the name
host_prompt_colors and edit it for the correct colors for your host prompt.

Read the comments in the file and run the colors script in this dir to see
a sample of all available colors.

